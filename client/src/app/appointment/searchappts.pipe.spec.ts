import { SearchapptsPipe } from './searchappts.pipe';

describe('SearchapptsPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchapptsPipe();
    expect(pipe).toBeTruthy();
  });
});
